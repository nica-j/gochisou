<span><%= link_to "全商品を表示", items_path, class:"mx-2" %></span>
      <% genres.each do |genre| %>
        <tr>
          <td>
            <%= link_to genre.name, genre_path(genre.id), class:"ec-genre-list-item" %>
          </td>
        </tr>
        <% end %>